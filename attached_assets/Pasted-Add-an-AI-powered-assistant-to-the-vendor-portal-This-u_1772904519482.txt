Add an AI-powered assistant to the vendor portal. This uses the Anthropic Claude API which is already available in the Replit environment.

STEP 1 — Add the assistant button to vendor pages

In frontend/vendor/dashboard.html and frontend/vendor/items.html, add a floating button in the bottom-right corner:
- A circle button, 56px, dark charcoal background (#38383B), white text, showing 💬
- Fixed position, bottom 80px (above the mobile bottom nav), right 16px
- z-index 200
- Clicking it opens the assistant panel

STEP 2 — Create frontend/vendor/assistant.html as a reusable panel

This is an include-style HTML snippet that gets injected into vendor pages. It is a slide-up panel (not a new page):

Panel layout:
- Slides up from the bottom on mobile, appears as a right-side panel on desktop (width 380px)
- Header: "Bowenstreet Assistant" with an X close button
- Chat message area: scrollable, shows conversation history
- At the bottom: a text input and a Send button, plus a 📷 camera/photo button
- The panel remembers conversation history while the page is open (in memory, not saved between sessions)

STEP 3 — Create app/routers/assistant.py

POST /api/v1/assistant/chat endpoint:
- Requires valid JWT (any role)
- Accepts: message (string), image_base64 (optional string), image_mime_type (optional string), vendor_context (optional object with vendor_id and current_page)
- Calls the Anthropic Messages API at https://api.anthropic.com/v1/messages
- Uses the API key from environment variable ANTHROPIC_API_KEY
- Uses model: claude-opus-4-6 (DO NOT hardcode a different model)
- System prompt (include all of this):

"""
You are the Bowenstreet Market vendor assistant. Bowenstreet Market is a vendor mall at 2837 Bowen St, Oshkosh WI 54901 with over 120 vendors selling handcrafted, vintage, and antique goods.

You help vendors with:
1. Adding items — walk them through: name, category, price, description, photos, sale dates, label type
2. Editing items — explain what each field does, remind them sale prices activate automatically by date
3. Archiving items — explain the difference between removing (hides from POS) and deleting
4. Understanding their sales and balance — explain how payouts work (deducted on 27th, paid on 1st)
5. Writing item descriptions — if given a description or photo, write a compelling, SEO-friendly product description
6. General questions about how the system works

When writing product descriptions:
- Start with the most important keyword naturally in the first sentence
- Include material, age/era if vintage, condition, dimensions if relevant
- Write 2-3 sentences, warm and inviting tone
- End with a detail that helps the buyer picture owning it
- Do NOT use the words "unique", "amazing", or "beautiful"

Keep responses concise and friendly. This is a mobile interface so avoid long walls of text.
If you see an image, describe what you see and suggest a product name, category, and description.
"""

- max_tokens: 500
- If ANTHROPIC_API_KEY is not set, return a 503 with message "Assistant not configured"
- Return: { reply: string }

STEP 4 — Add image analysis support

When the vendor taps the 📷 button in the assistant:
- Opens the device camera or photo library (same as the item photo upload: accept="image/*" capture="environment")
- The selected image is converted to base64 in JavaScript
- Sent along with the message "What is this item? Suggest a name, category, price range, and description."
- The API sends it to Claude as a vision message with the image included
- Claude's response appears in the chat

JavaScript to convert image to base64:
```javascript
const reader = new FileReader();
reader.onload = (e) => {
  const base64 = e.target.result.split(',')[1];
  const mime = file.type;
  // send to API with image_base64 and image_mime_type
};
reader.readAsDataURL(file);
```

In the backend, build the Claude message like this when an image is present:
```python
content = [
    {"type": "image", "source": {"type": "base64", "media_type": image_mime_type, "data": image_base64}},
    {"type": "text", "text": message}
]
```

STEP 5 — Wire the assistant router into app/main.py

Add: app.include_router(assistant.router)

STEP 6 — Add ANTHROPIC_API_KEY to the environment

In Replit Secrets, add:
- Key: ANTHROPIC_API_KEY
- Value: (leave blank for now, Kevin will add this)

Add ANTHROPIC_API_KEY to app/config.py as an optional setting with default None.

STEP 7 — Update frontend/static/js/api.js

Add this function:
```javascript
async function apiChat(message, imageBase64 = null, imageMimeType = null) {
  const body = { message };
  if (imageBase64) {
    body.image_base64 = imageBase64;
    body.image_mime_type = imageMimeType;
  }
  return await apiPost('/api/v1/assistant/chat', body);
}
```

STEP 8 — Inject the assistant panel into vendor pages

At the bottom of frontend/vendor/dashboard.html and frontend/vendor/items.html, add:
- The floating 💬 button
- The assistant panel HTML inline (no iframe, no separate page load)
- The JavaScript to handle open/close, send message, receive reply, display in chat bubbles
- Vendor messages appear on the right (dark background), assistant replies on the left (lighter background)
- Show a typing indicator (three dots) while waiting for the API response
- If the API returns an error or 503, show: "Assistant is not available right now."

STEP 9 — Test

Confirm:
1. The 💬 button appears on vendor dashboard and items page
2. Clicking it opens the panel
3. Typing a message and sending returns a response (will fail gracefully if API key not set)
4. The photo button opens camera on mobile
5. The panel closes with the X button
6. On desktop the panel appears on the right side, on mobile it slides up from the bottom

Report what is complete.