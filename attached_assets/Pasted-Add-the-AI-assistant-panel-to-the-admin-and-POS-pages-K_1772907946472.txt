Add the AI assistant panel to the admin and POS pages. Keep it simple — same panel that exists on vendor pages, just available in those areas too.

STEP 1 — Add the assistant panel to frontend/admin/index.html

Copy the exact same assistant panel HTML, CSS, and JavaScript that exists on vendor/items.html into admin/index.html. Place it at the bottom of the page before the closing body tag.

The floating 💬 button should appear in the bottom-right corner, same position as vendor pages.

Update the system prompt context sent with admin messages to include:
"The user is an admin at Bowenstreet Market."

STEP 2 — Add the assistant panel to frontend/admin/vendors.html

Same as Step 1. Add the full assistant panel to admin/vendors.html.

Context: "The user is an admin managing vendor accounts at Bowenstreet Market."

STEP 3 — Add the assistant panel to frontend/pos/index.html

Same panel, placed at the bottom before closing body tag.

The floating button should be positioned so it does not overlap the cart or payment buttons — place it at bottom-right but with enough bottom margin to clear the POS interface (bottom: 120px instead of 80px).

Context: "The user is a cashier at the POS register at Bowenstreet Market."

STEP 4 — Create a shared assistant panel include

To avoid duplicating the code across every page, extract the assistant panel into a single shared file: frontend/static/js/assistant-panel.js

This file should contain a function called initAssistantPanel(context) that:
- Injects the assistant panel HTML into the current page
- Injects the required CSS if not already present
- Sets the context string that gets sent with every message
- Attaches all event listeners

Then on each page that needs the assistant, replace the inline panel code with:
<script src="/static/js/assistant-panel.js"></script>
<script>initAssistantPanel("The user is an admin at Bowenstreet Market.");</script>

Do this for all pages: admin/index.html, admin/vendors.html, pos/index.html, vendor/dashboard.html, vendor/items.html

STEP 5 — Update the assistant API to handle admin and cashier tool calls

In app/routers/assistant.py, the tool calls (add_item, edit_item, etc.) currently assume the user is a vendor and use their vendor_id.

Add this logic:
- If the authenticated user is a vendor: use their own vendor_id for item operations (existing behavior)
- If the authenticated user is admin or cashier: the tools require a vendor_id to be specified in the conversation, or the assistant should ask "Which vendor's items should I look at?" before proceeding with inventory tools

Update the system prompt section for admin/cashier context:
"""
If the user is an admin or cashier, inventory tool calls require knowing which vendor to act on.
If a vendor is not specified, ask: "Which vendor would you like to manage items for?"
You can call list_items without a vendor filter to show all items across all vendors for admin users.
"""

STEP 6 — Restart and confirm:

1. The 💬 button appears on admin/index.html
2. The 💬 button appears on admin/vendors.html  
3. The 💬 button appears on pos/index.html and does not overlap the register UI
4. Clicking the button on any page opens the assistant panel
5. Sending a message returns a response
6. The vendor pages still work as before