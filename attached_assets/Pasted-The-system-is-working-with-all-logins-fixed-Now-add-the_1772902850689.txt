The system is working with all logins fixed. Now add the following features:

PHASE 3A — CASHIER PERMISSIONS

STEP 1 — Update the cashier role so they can access:
- /pos/index.html (already works)
- /admin/vendors.html — read only, no add/edit/delete buttons visible
- /vendor/items.html — full access: add, edit, delete, print labels for any vendor's items
- A new page: /admin/customers.html (build this in Phase 3B below)

In auth.py, cashier is already a valid role. Make sure the require_cashier_or_admin dependency allows both cashier and admin roles to access the items router write endpoints.

STEP 2 — Update frontend/vendor/items.html so that:
- If logged in as admin or cashier: show a vendor filter dropdown at the top so they can switch between vendors and see that vendor's items
- If logged in as vendor: no dropdown, only see own items

---

PHASE 3B — RECEIPT PRINTING (Star TSP100III)

The Star TSP100III printer uses ESC/POS commands and connects via USB or network. Browsers cannot send raw ESC/POS directly, so we use the Star CloudPRNT protocol or print via the browser print dialog with a thermal-optimized layout.

STEP 3 — Update the POS receipt to support thermal printing:
- Add a Print Receipt button on the sale success screen
- When clicked, open a new browser window with a thermal-receipt-formatted page
- The page should use @media print CSS: 80mm wide, no margins, font-size 12px, monospace font
- Receipt layout (top to bottom):
  * Store name: BOWENSTREET MARKET centered bold
  * Address: 2837 Bowen St, Oshkosh WI 54901
  * Blank line
  * Date and time of sale
  * Cashier name
  * Divider line (dashes)
  * Each item: item name left-aligned, price right-aligned, vendor booth below item name in smaller text
  * Divider line
  * Subtotal right-aligned
  * Tax (5.5%) right-aligned
  * Total right-aligned bold
  * Payment method
  * If cash: Amount tendered and Change due
  * Blank lines
  * Thank you for shopping at Bowenstreet Market! centered
  * Visit us at bowenstreetmarket.com centered
  * Blank lines (for paper cut)
- Auto-trigger window.print() when the receipt window loads
- The Star TSP100III will print automatically if set as the default printer in Windows

---

PHASE 3C — DYMO LABELWRITER 450 SUPPORT

The Dymo LabelWriter 450 prints labels using the Dymo Label Framework (DLS). Browsers can connect to it via the Dymo Web Service that runs locally on the computer.

STEP 4 — Update app/services/labels.py to generate a Dymo-compatible label:
- Keep the existing ReportLab label generator for PDF batch sheets
- Add a new function generate_dymo_xml(item) that returns a Dymo Label XML string
- The Dymo label should be sized for 30252 Address labels (1-1/8" x 3-1/2") OR 30336 labels (1" x 2-1/8") — make the size a config setting in app/config.py with default 30336
- Label contents: item name, price (active_price), booth number, barcode as text (the barcode number)
- Note: actual barcode graphic is handled by Dymo's own rendering via the XML

STEP 5 — Add a new API endpoint:
- GET /api/v1/items/{id}/dymo-label — returns the Dymo XML for that item as text/xml

STEP 6 — Update the Print Label button on frontend/vendor/items.html:
- Change it to a dropdown: "Print PDF Label" and "Print Dymo Label"
- PDF Label: existing behavior, opens PDF in new tab
- Dymo Label: 
  1. Fetch the Dymo XML from the API
  2. Call the Dymo Web Service at http://localhost:41951/DYMO/DLS/Printing/PrintLabel
  3. If the Dymo Web Service is not running, show a friendly error: "Dymo software is not running. Please open Dymo Connect on this computer."
  4. On success, show "Label sent to Dymo printer"

---

PHASE 3D — VENDOR LABEL PREFERENCE

STEP 7 — Add a per-item label preference setting:
- Add a column to the items table: label_style VARCHAR(20) DEFAULT 'standard' (values: standard, dymo)
- Add this field to ItemUpdate schema and items router PUT endpoint
- In frontend/vendor/items.html, on the Add/Edit item form, add a toggle or select: "Label Type: Standard PDF / Dymo"
- The value selected is saved with the item and remembered between logins because it is stored in the database
- When Print Label is clicked, automatically use the label_style saved on that item (no dropdown needed if preference is already set)

---

PHASE 3E — CUSTOMERS PAGE FOR CASHIERS

STEP 8 — Create frontend/admin/customers.html:
- Accessible by admin and cashier roles
- Shows a searchable table of all vendors (name, email, phone, booth number, balance)
- Search bar filters by name, email, or booth number as you type
- Clicking a vendor row expands it to show: their items list (name, price, status), their recent sales history (last 5 sales), their current balance
- This is read-only — no edit buttons
- Add a link to this page in the cashier's POS terminal navbar and in the admin navbar

---

STEP 9 — Add a database migration for the new label_style column:
Create migrations/002_add_label_style.sql with:
ALTER TABLE items ADD COLUMN IF NOT EXISTS label_style VARCHAR(20) DEFAULT 'standard';
Run this migration against the database.

STEP 10 — Restart the server and confirm:
1. Cashier can view vendors (read only) and manage items (full access)
2. Receipt prints correctly from the POS terminal
3. Dymo label endpoint returns valid XML
4. PDF label still works
5. Label style preference saves and persists
6. Customers page loads for cashier and admin