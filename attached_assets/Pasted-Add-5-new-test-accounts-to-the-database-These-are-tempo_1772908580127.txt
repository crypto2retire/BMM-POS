Add 5 new test accounts to the database. These are temporary testing accounts and will be deleted before go-live.

STEP 1 — Update scripts/seed_dev.py

Add a new function called seed_test_accounts() that creates these accounts if they do not already exist (check by email before inserting):

Full access accounts (role: admin):
1. Name: Nora, email: nora@email.com, password: vendor123, role: admin, booth: TEST-1, monthly_rent: 0
2. Name: Sammy, email: sammy@email.com, password: vendor123, role: admin, booth: TEST-2, monthly_rent: 0
3. Name: Ashley, email: ashley@email.com, password: vendor123, role: admin, booth: TEST-3, monthly_rent: 0
4. Name: Anne, email: anne@email.com, password: vendor123, role: admin, booth: TEST-4, monthly_rent: 0

Vendor access only (role: vendor):
5. Name: Paula, email: paula@email.com, password: vendor123, role: vendor, booth: TEST-5, monthly_rent: 0

For each account:
- Hash the password using the same bcrypt method used for all other accounts
- Set status: active
- Set payout_method: zelle
- Check if email already exists before inserting — skip with a message if it does

At the end of the function, print a summary table:
Email | Role | Status
nora@email.com | admin | created (or already exists)
sammy@email.com | admin | created (or already exists)
ashley@email.com | admin | created (or already exists)
anne@email.com | admin | created (or already exists)
paula@email.com | vendor | created (or already exists)

STEP 2 — Run seed_test_accounts()

Call seed_test_accounts() at the bottom of the seed script and run the script now.

STEP 3 — Verify all 5 accounts

After the script runs, query the database and confirm all 5 emails exist with the correct roles.

STEP 4 — Test logins

Test that these accounts can log in and land on the correct page:
- nora@email.com → admin dashboard
- sammy@email.com → admin dashboard
- ashley@email.com → admin dashboard
- anne@email.com → admin dashboard
- paula@email.com → vendor dashboard

Report which accounts were created and which logins succeed.