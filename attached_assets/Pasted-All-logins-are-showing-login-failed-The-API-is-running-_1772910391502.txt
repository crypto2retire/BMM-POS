All logins are showing "login failed". The API is running but authentication is broken. Fix it completely.

STEP 1 — Test the login API directly from the server

Run these commands and print the full output of each:

curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@bowenstreetmarket.com&password=admin123" | python3 -m json.tool

Then also run:
curl -s -o /dev/null -w "%{http_code}" -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@bowenstreetmarket.com&password=admin123"

Print the HTTP status code and full response body.

STEP 2 — Check the database directly

Run this query and print results:
SELECT id, name, email, role, status, 
       length(password_hash) as hash_length,
       left(password_hash, 7) as hash_prefix
FROM vendors 
WHERE email IN (
  'admin@bowenstreetmarket.com',
  'cashier@bowenstreetmarket.com', 
  'sarah@email.com'
);

This tells us:
- Whether the accounts exist
- Whether password hashes are present and look correct (bcrypt hashes start with $2b$ and are 60 chars long)

STEP 3 — Check app/routers/auth.py

Print the entire file. Specifically look for:
- The login function — does it use OAuth2PasswordRequestForm?
- The password verification call — is it using pwd_context.verify(plain, hashed)?
- Any recent changes that might have broken it

STEP 4 — Check frontend/vendor/login.html

Print the entire JavaScript login section. Look for:
- What URL is it posting to?
- What Content-Type header is it sending?
- What field names is it using? (MUST be username= and password= for OAuth2, NOT email=)
- How is it handling the response?
- What exactly triggers the "login failed" message?

STEP 5 — Fix all issues found

Most likely causes in order of probability:

A) Frontend is sending email= instead of username= 
Fix: Change the fetch body to use username not email:
const formData = new URLSearchParams();
formData.append('username', emailValue);
formData.append('password', passwordValue);

B) Password hashes in database are corrupted or wrong format
Fix: Reset all passwords with fresh bcrypt hashes:
UPDATE vendors SET password_hash = '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TgxwjxVKBF3FG8xCfGPMFn3nKjWe' WHERE email = 'admin@bowenstreetmarket.com';
(This sets password to admin123 — generate fresh hashes using passlib)

C) A broken import is causing a 500 error on the login endpoint
Fix: Check for any ImportError in startup logs and fix the broken import

D) The auth router is not registered in main.py
Fix: Add app.include_router(auth.router, prefix="/api/v1") to main.py

STEP 6 — Reset all passwords to be safe

Regardless of what else is found, run this Python script to reset every account password fresh:

python3 -c "
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto')
hash = pwd_context.hash('vendor123')
admin_hash = pwd_context.hash('admin123')
cashier_hash = pwd_context.hash('cashier123')
print('vendor123 hash:', hash)
print('admin123 hash:', admin_hash)
print('cashier123 hash:', cashier_hash)
"

Then update the database with the fresh hashes for each account.

STEP 7 — Rebuild the login page fetch from scratch

Replace the login form JavaScript in frontend/vendor/login.html with this exact code:

async function handleLogin(event) {
  event.preventDefault();
  
  const email = document.getElementById('email').value.trim();
  const password = document.getElementById('password').value;
  const errorDiv = document.getElementById('login-error');
  
  errorDiv.style.display = 'none';
  
  try {
    const formData = new URLSearchParams();
    formData.append('username', email);
    formData.append('password', password);
    
    const response = await fetch('/api/v1/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: formData.toString()
    });
    
    if (!response.ok) {
      const err = await response.json();
      errorDiv.textContent = err.detail || 'Login failed. Check your email and password.';
      errorDiv.style.display = 'block';
      return;
    }
    
    const data = await response.json();
    sessionStorage.setItem('bmm_token', data.access_token);
    
    const payload = JSON.parse(atob(data.access_token.split('.')[1]));
    const role = payload.role;
    
    if (role === 'admin') {
      window.location.href = '/admin/index.html';
    } else if (role === 'cashier') {
      window.location.href = '/pos/index.html';
    } else {
      window.location.href = '/vendor/dashboard.html';
    }
    
  } catch (err) {
    errorDiv.textContent = 'Connection error. Please try again.';
    errorDiv.style.display = 'block';
  }
}

Make sure the login form calls handleLogin on submit:
<form onsubmit="handleLogin(event)">

STEP 8 — Restart and test all accounts

After all fixes, test every account and report:
- HTTP status code returned for each login attempt
- Whether the token is received
- Whether the redirect works

Test accounts:
admin@bowenstreetmarket.com / admin123
cashier@bowenstreetmarket.com / cashier123
sarah@email.com / vendor123
nora@email.com / vendor123
paula@email.com / vendor123